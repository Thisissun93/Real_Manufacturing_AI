project:
  name: Real_Manufacturing_AI

data:
  sample_size: 5000
  random_state: 42

model:
  algorithm: RandomForest
  n_estimators: 200
  test_size: 0.2

pipeline:
  save_images: true
  save_reports: true
  save_model: true

dashboard:
  default_yield_min: 0
  default_yield_max: 100